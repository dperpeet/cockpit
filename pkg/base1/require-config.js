(function(require) {
    require.config({
        baseUrl: "../",
        waitSeconds: 30,
        paths: {
            "jquery": "base1/jquery",
            "selectize": "base1/jquery"
        }
    });
})(require);

